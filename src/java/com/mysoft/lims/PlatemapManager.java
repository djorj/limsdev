// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.lims;

import java.sql.*;
// imports+ 

// imports- 

/**
 * Handles database calls for the platemap table.
 */
public class PlatemapManager
// extends+ 

// extends- 
{

    /**
     * Column plateid of type Types.BIGINT mapped to Long.
     */
    public static final int ID_PLATEID = 0;
    public static final int TYPE_PLATEID = Types.BIGINT;
    public static final String NAME_PLATEID = "plateid";

    /**
     * Column wellno of type Types.VARCHAR mapped to String.
     */
    public static final int ID_WELLNO = 1;
    public static final int TYPE_WELLNO = Types.VARCHAR;
    public static final String NAME_WELLNO = "wellno";

    /**
     * Column result of type Types.VARCHAR mapped to String.
     */
    public static final int ID_RESULT = 2;
    public static final int TYPE_RESULT = Types.VARCHAR;
    public static final String NAME_RESULT = "result";

    /**
     * Column rawdata of type Types.VARCHAR mapped to String.
     */
    public static final int ID_RAWDATA = 3;
    public static final int TYPE_RAWDATA = Types.VARCHAR;
    public static final String NAME_RAWDATA = "rawdata";

    /**
     * Column barcode of type Types.VARCHAR mapped to String.
     */
    public static final int ID_BARCODE = 4;
    public static final int TYPE_BARCODE = Types.VARCHAR;
    public static final String NAME_BARCODE = "barcode";


    private static final String TABLE_NAME = "platemap";

    /**
     * Create an array of type string containing all the fields of the platemap table.
     */
    private static final String[] FIELD_NAMES = 
    {
        "platemap.plateid"
        ,"platemap.wellno"
        ,"platemap.result"
        ,"platemap.rawdata"
        ,"platemap.barcode"
    };

    /**
     * Field that contains the comma separated fields of the platemap table.
     */
    private static final String ALL_FIELDS = "platemap.plateid"
                            + ",platemap.wellno"
                            + ",platemap.result"
                            + ",platemap.rawdata"
                            + ",platemap.barcode";

    private static PlatemapManager singleton = new PlatemapManager();

    /**
     * Get the PlatemapManager singleton.
     *
     * @return PlatemapManager 
     */
    synchronized public static PlatemapManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own PlatemapManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(PlatemapManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new PlatemapBean instance.
     *
     * @return the new PlatemapBean 
     */
    public PlatemapBean createPlatemapBean()
    {
        return new PlatemapBean();
    }

    //////////////////////////////////////
    // PRIMARY KEY METHODS
    //////////////////////////////////////

    /**
     * Loads a PlatemapBean from the platemap using its key fields.
     *
     * @return a unique PlatemapBean 
     */
    //12
    public PlatemapBean loadByPrimaryKey(String wellno) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM platemap WHERE platemap.wellno=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            ps.setString(1, wellno);
            PlatemapBean pReturn[] = loadByPreparedStatement(ps);
            if (pReturn.length < 1)
                return null;
            else
                return pReturn[0];
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    /**
     * Deletes rows according to its keys.
     *
     * @return the number of deleted rows
     */
    //60
    public int deleteByPrimaryKey(String wellno) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE from platemap WHERE platemap.wellno=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            ps.setString(1, wellno);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    
    
    //////////////////////////////////////
    // FOREIGN KEY METHODS 
    //////////////////////////////////////

    /**
     * Loads PlatemapBean array from the platemap table using its plateid field.
     *
     * @return an array of PlatemapBean 
     */
    // LOAD BY IMPORTED KEY
    public PlatemapBean[] loadByPlateid(Long value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM platemap WHERE plateid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setLong(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the platemap table by plateid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByPlateid(Long value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM platemap WHERE plateid=?");
            Manager.setLong(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    //////////////////////////////////////
    // GET/SET FOREIGN KEY BEAN METHOD
    //////////////////////////////////////
    /**
     * Retrieves the PlateBean object from the platemap.plateid field.
     *
     * @param pObject the PlatemapBean 
     * @return the associated PlateBean pObject
     */
    // GET IMPORTED
    public PlateBean getPlateBean(PlatemapBean pObject) throws SQLException
    {
        PlateBean other = PlateManager.getInstance().createPlateBean();
        other.setPlateid(pObject.getPlateid());
        return PlateManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the PlatemapBean object to the PlateBean object.
     *
     * @param pObject the PlatemapBean object to use
     * @param pObjectToBeSet the PlateBean object to associate to the PlatemapBean 
     * @return the associated PlateBean pObject
     */
    // SET IMPORTED
    public PlatemapBean setPlateBean(PlatemapBean pObject,PlateBean pObjectToBeSet)
    {
        pObject.setPlateid(pObjectToBeSet.getPlateid());
        return pObject;
    }



    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from platemap.
     *
     * @return an array of PlatemapManager pObject
     */
    //38
    public PlatemapBean[] loadAll() throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM platemap",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of PlatemapBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting PlatemapBean table 
     */
    //49
    public PlatemapBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of PlatemapBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting PlatemapBean table 
     */
    //51
    public PlatemapBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from platemap " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from platemap ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try 
        {
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else
                    v.add(decodeRow(rs, fieldList));
            }

            return (PlatemapBean[])v.toArray(new PlatemapBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from platemap table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the platemap table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records. 
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM platemap " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the PlatemapBean pObject into the database.
     *
     * @param pObject the PlatemapBean pObject to be saved
     */
    //100
    public PlatemapBean save(PlatemapBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE 
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into platemap (");
    
                if (pObject.isPlateidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("plateid");
                    _dirtyCount++;
                }

                if (pObject.isWellnoModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("wellno");
                    _dirtyCount++;
                }

                if (pObject.isResultModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("result");
                    _dirtyCount++;
                }

                if (pObject.isRawdataModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("rawdata");
                    _dirtyCount++;
                }

                if (pObject.isBarcodeModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("barcode");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isPlateidModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPlateid());
                }
    
                if (pObject.isWellnoModified()) {
                    ps.setString(++_dirtyCount, pObject.getWellno());
                }
    
                if (pObject.isResultModified()) {
                    ps.setString(++_dirtyCount, pObject.getResult());
                }
    
                if (pObject.isRawdataModified()) {
                    ps.setString(++_dirtyCount, pObject.getRawdata());
                }
    
                if (pObject.isBarcodeModified()) {
                    ps.setString(++_dirtyCount, pObject.getBarcode());
                }
    
                ps.executeUpdate();
    
                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else 
            { // UPDATE 
                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE platemap SET ");
                boolean useComma=false;

                if (pObject.isPlateidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("plateid").append("=?");
                }

                if (pObject.isWellnoModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("wellno").append("=?");
                }

                if (pObject.isResultModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("result").append("=?");
                }

                if (pObject.isRawdataModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("rawdata").append("=?");
                }

                if (pObject.isBarcodeModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("barcode").append("=?");
                }
                _sql.append(" WHERE ");
                _sql.append("platemap.wellno=?");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isPlateidModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getPlateid());
                }

                if (pObject.isWellnoModified()) {
                      ps.setString(++_dirtyCount, pObject.getWellno());
                }

                if (pObject.isResultModified()) {
                      ps.setString(++_dirtyCount, pObject.getResult());
                }

                if (pObject.isRawdataModified()) {
                      ps.setString(++_dirtyCount, pObject.getRawdata());
                }

                if (pObject.isBarcodeModified()) {
                      ps.setString(++_dirtyCount, pObject.getBarcode());
                }
    
                if (_dirtyCount == 0) {
                     return pObject;
                }
    
                ps.setString(++_dirtyCount, pObject.getWellno());
                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }
    
            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of PlatemapBean pObjects into the database.
     *
     * @param pObjects the PlatemapBean pObject table to be saved
     * @return the saved PlatemapBean array.
     */
    //65
    public PlatemapBean[] save(PlatemapBean[] pObjects) throws SQLException 
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique PlatemapBean pObject from a template one giving a c
     *
     * @param pObject the PlatemapBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public PlatemapBean loadUniqueUsingTemplate(PlatemapBean pObject) throws SQLException
    {
         PlatemapBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of PlatemapBean from a template one.
     *
     * @param pObject the PlatemapBean template to look for
     * @return all the PlatemapBean matching the template
     */
    //88
    public PlatemapBean[] loadUsingTemplate(PlatemapBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from platemap WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;
    
             if (pObject.isPlateidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("plateid= ?");
             }
    
             if (pObject.isWellnoModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("wellno= ?");
             }
    
             if (pObject.isResultModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("result= ?");
             }
    
             if (pObject.isRawdataModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("rawdata= ?");
             }
    
             if (pObject.isBarcodeModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("barcode= ?");
             }
    
             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;
    
             if (pObject.isPlateidModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getPlateid());
             }
    
             if (pObject.isWellnoModified()) {
                 ps.setString(++_dirtyCount, pObject.getWellno());
             }
    
             if (pObject.isResultModified()) {
                 ps.setString(++_dirtyCount, pObject.getResult());
             }
    
             if (pObject.isRawdataModified()) {
                 ps.setString(++_dirtyCount, pObject.getRawdata());
             }
    
             if (pObject.isBarcodeModified()) {
                 ps.setString(++_dirtyCount, pObject.getBarcode());
             }
    
             ps.executeQuery();
             return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a PlatemapBean template.
     *
     * @param pObject the PlatemapBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(PlatemapBean pObject) throws SQLException
    {
        if (pObject.isWellnoInitialized())
            return deleteByPrimaryKey(pObject.getWellno());
    
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;
    
        try 
        {
            sql = new StringBuffer("DELETE FROM platemap WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isPlateidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("plateid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isWellnoInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("wellno").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isResultInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("result").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isRawdataInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("rawdata").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isBarcodeInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("barcode").append("=?");
                _dirtyAnd ++;
            }
    
            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;
    
            if (pObject.isPlateidInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getPlateid());
            }
    
            if (pObject.isWellnoInitialized()) {
                ps.setString(++_dirtyCount, pObject.getWellno());
            }
    
            if (pObject.isResultInitialized()) {
                ps.setString(++_dirtyCount, pObject.getResult());
            }
    
            if (pObject.isRawdataInitialized()) {
                ps.setString(++_dirtyCount, pObject.getRawdata());
            }
    
            if (pObject.isBarcodeInitialized()) {
                ps.setString(++_dirtyCount, pObject.getBarcode());
            }
    
            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT 
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table platemap.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table platemap with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from platemap " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;    
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table platemap with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific PlatemapBean pObject given a c
     *
     * @param pObject the PlatemapBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(PlatemapBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;
    
        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from platemap WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;
    
                if (pObject.isPlateidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("plateid= ?");
                }
    
                if (pObject.isWellnoModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("wellno= ?");
                }
    
                if (pObject.isResultModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("result= ?");
                }
    
                if (pObject.isRawdataModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("rawdata= ?");
                }
    
                if (pObject.isBarcodeModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("barcode= ?");
                }
    
                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");
    
                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
    
                _dirtyCount = 0;
    
                if (pObject.isPlateidModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPlateid());
                }
    
                if (pObject.isWellnoModified()) {
                    ps.setString(++_dirtyCount, pObject.getWellno());
                }
    
                if (pObject.isResultModified()) {
                    ps.setString(++_dirtyCount, pObject.getResult());
                }
    
                if (pObject.isRawdataModified()) {
                    ps.setString(++_dirtyCount, pObject.getRawdata());
                }
    
                if (pObject.isBarcodeModified()) {
                    ps.setString(++_dirtyCount, pObject.getBarcode());
                }
    
                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the platemap on a PlatemapBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting PlatemapBean pObject
     */
    //72
    public PlatemapBean decodeRow(ResultSet rs) throws SQLException
    {
        PlatemapBean pObject = createPlatemapBean();
        pObject.setPlateid(Manager.getLong(rs, 1));
        pObject.setWellno(rs.getString(2));
        pObject.setResult(rs.getString(3));
        pObject.setRawdata(rs.getString(4));
        pObject.setBarcode(rs.getString(5));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the platemap table on a PlatemapBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting PlatemapBean pObject
     */
    //73
    public PlatemapBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        PlatemapBean pObject = createPlatemapBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_PLATEID:
                    ++pos;
                    pObject.setPlateid(Manager.getLong(rs, pos));
                    break;
                case ID_WELLNO:
                    ++pos;
                    pObject.setWellno(rs.getString(pos));
                    break;
                case ID_RESULT:
                    ++pos;
                    pObject.setResult(rs.getString(pos));
                    break;
                case ID_RAWDATA:
                    ++pos;
                    pObject.setRawdata(rs.getString(pos));
                    break;
                case ID_BARCODE:
                    ++pos;
                    pObject.setBarcode(rs.getString(pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of PlatemapBean 
     */
    //41
    public PlatemapBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of PlatemapBean 
     */
    public PlatemapBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else 
                    v.add(decodeRow(rs, fieldList));
            }
            return (PlatemapBean[])v.toArray(new PlatemapBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER 
    ///////////////////////////////////////////////////////////////////////
    private PlatemapListener listener = null;

    /**
     * Registers a unique PlatemapListener listener.
     */
    //66.5
    public void registerListener(PlatemapListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the PlatemapBean pObject.
     *
     * @param pObject the PlatemapBean pObject to be saved
     */
    //67
    void beforeInsert(PlatemapBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the PlatemapBean pObject.
     *
     * @param pObject the PlatemapBean pObject to be saved
     */
    //68
    void afterInsert(PlatemapBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the PlatemapBean pObject.
     *
     * @param pObject the PlatemapBean pObject to be updated
     */
    //69
    void beforeUpdate(PlatemapBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the PlatemapBean pObject.
     *
     * @param pObject the PlatemapBean pObject to be updated
     */
    //70
    void afterUpdate(PlatemapBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS  
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 
}
