// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.lims;

import java.util.Comparator;
// imports+ 

// imports- 

/**
 * Comparator class is used to sort the InvoicetypeBean objects.
 */
public class InvoicetypeComparator implements Comparator
// extends+ 

// extends- 
{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for InvoicetypeComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new InvoicetypeComparator(InvoicetypeManager.INVOICETYPEID, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>InvoicetypeManager.ID_INVOICETYPEID
     *   <li>InvoicetypeManager.ID_NAME
     * </ul>
     */
    public InvoicetypeComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for InvoicetypeComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new InvoicetypeComparator(InvoicetypeManager.INVOICETYPEID, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>InvoicetypeManager.ID_INVOICETYPEID
     *   <li>InvoicetypeManager.ID_NAME
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public InvoicetypeComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        InvoicetypeBean b1 = (InvoicetypeBean)pObj1;
        InvoicetypeBean b2 = (InvoicetypeBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case InvoicetypeManager.ID_INVOICETYPEID:
                if (b1.getInvoicetypeid() == null && b2.getInvoicetypeid() != null) {
                    iReturn = -1;
                } else if (b1.getInvoicetypeid() == null && b2.getInvoicetypeid() == null) {
                    iReturn = 0;
                } else if (b1.getInvoicetypeid() != null && b2.getInvoicetypeid() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getInvoicetypeid().compareTo(b2.getInvoicetypeid());
                }
                break;
            case InvoicetypeManager.ID_NAME:
                if (b1.getName() == null && b2.getName() != null) {
                    iReturn = -1;
                } else if (b1.getName() == null && b2.getName() == null) {
                    iReturn = 0;
                } else if (b1.getName() != null && b2.getName() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getName().compareTo(b2.getName());
                }
                break;
            default: 
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }
// class+ 

// class- 
}
