// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.lims;

import java.sql.*;
// imports+ 

// imports- 

/**
 * Handles database calls for the sampletypederivatetype table.
 */
public class SampletypederivatetypeManager
// extends+ 

// extends- 
{

    /**
     * Column sampletypeid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_SAMPLETYPEID = 0;
    public static final int TYPE_SAMPLETYPEID = Types.INTEGER;
    public static final String NAME_SAMPLETYPEID = "sampletypeid";

    /**
     * Column derivatetypeid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_DERIVATETYPEID = 1;
    public static final int TYPE_DERIVATETYPEID = Types.INTEGER;
    public static final String NAME_DERIVATETYPEID = "derivatetypeid";

    /**
     * Column standart of type Types.BIT mapped to Boolean.
     */
    public static final int ID_STANDART = 2;
    public static final int TYPE_STANDART = Types.BIT;
    public static final String NAME_STANDART = "standart";


    private static final String TABLE_NAME = "sampletypederivatetype";

    /**
     * Create an array of type string containing all the fields of the sampletypederivatetype table.
     */
    private static final String[] FIELD_NAMES = 
    {
        "sampletypederivatetype.sampletypeid"
        ,"sampletypederivatetype.derivatetypeid"
        ,"sampletypederivatetype.standart"
    };

    /**
     * Field that contains the comma separated fields of the sampletypederivatetype table.
     */
    private static final String ALL_FIELDS = "sampletypederivatetype.sampletypeid"
                            + ",sampletypederivatetype.derivatetypeid"
                            + ",sampletypederivatetype.standart";

    private static SampletypederivatetypeManager singleton = new SampletypederivatetypeManager();

    /**
     * Get the SampletypederivatetypeManager singleton.
     *
     * @return SampletypederivatetypeManager 
     */
    synchronized public static SampletypederivatetypeManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own SampletypederivatetypeManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(SampletypederivatetypeManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new SampletypederivatetypeBean instance.
     *
     * @return the new SampletypederivatetypeBean 
     */
    public SampletypederivatetypeBean createSampletypederivatetypeBean()
    {
        return new SampletypederivatetypeBean();
    }

    //////////////////////////////////////
    // PRIMARY KEY METHODS
    //////////////////////////////////////

    /**
     * Loads a SampletypederivatetypeBean from the sampletypederivatetype using its key fields.
     *
     * @return a unique SampletypederivatetypeBean 
     */
    //12
    public SampletypederivatetypeBean loadByPrimaryKey(Integer sampletypeid, Integer derivatetypeid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM sampletypederivatetype WHERE sampletypederivatetype.sampletypeid=? and sampletypederivatetype.derivatetypeid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, sampletypeid);
            Manager.setInteger(ps, 2, derivatetypeid);
            SampletypederivatetypeBean pReturn[] = loadByPreparedStatement(ps);
            if (pReturn.length < 1)
                return null;
            else
                return pReturn[0];
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    /**
     * Deletes rows according to its keys.
     *
     * @return the number of deleted rows
     */
    //60
    public int deleteByPrimaryKey(Integer sampletypeid, Integer derivatetypeid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE from sampletypederivatetype WHERE sampletypederivatetype.sampletypeid=? and sampletypederivatetype.derivatetypeid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, sampletypeid);
            Manager.setInteger(ps, 2, derivatetypeid);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    
    
    //////////////////////////////////////
    // FOREIGN KEY METHODS 
    //////////////////////////////////////

    /**
     * Loads SampletypederivatetypeBean array from the sampletypederivatetype table using its sampletypeid field.
     *
     * @return an array of SampletypederivatetypeBean 
     */
    // LOAD BY IMPORTED KEY
    public SampletypederivatetypeBean[] loadBySampletypeid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM sampletypederivatetype WHERE sampletypeid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the sampletypederivatetype table by sampletypeid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteBySampletypeid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM sampletypederivatetype WHERE sampletypeid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    //////////////////////////////////////
    // GET/SET FOREIGN KEY BEAN METHOD
    //////////////////////////////////////
    /**
     * Retrieves the SampletypeBean object from the sampletypederivatetype.sampletypeid field.
     *
     * @param pObject the SampletypederivatetypeBean 
     * @return the associated SampletypeBean pObject
     */
    // GET IMPORTED
    public SampletypeBean getSampletypeBean(SampletypederivatetypeBean pObject) throws SQLException
    {
        SampletypeBean other = SampletypeManager.getInstance().createSampletypeBean();
        other.setSampletypeid(pObject.getSampletypeid());
        return SampletypeManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the SampletypederivatetypeBean object to the SampletypeBean object.
     *
     * @param pObject the SampletypederivatetypeBean object to use
     * @param pObjectToBeSet the SampletypeBean object to associate to the SampletypederivatetypeBean 
     * @return the associated SampletypeBean pObject
     */
    // SET IMPORTED
    public SampletypederivatetypeBean setSampletypeBean(SampletypederivatetypeBean pObject,SampletypeBean pObjectToBeSet)
    {
        pObject.setSampletypeid(pObjectToBeSet.getSampletypeid());
        return pObject;
    }



    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from sampletypederivatetype.
     *
     * @return an array of SampletypederivatetypeManager pObject
     */
    //38
    public SampletypederivatetypeBean[] loadAll() throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM sampletypederivatetype",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of SampletypederivatetypeBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting SampletypederivatetypeBean table 
     */
    //49
    public SampletypederivatetypeBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of SampletypederivatetypeBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting SampletypederivatetypeBean table 
     */
    //51
    public SampletypederivatetypeBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from sampletypederivatetype " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from sampletypederivatetype ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try 
        {
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else
                    v.add(decodeRow(rs, fieldList));
            }

            return (SampletypederivatetypeBean[])v.toArray(new SampletypederivatetypeBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from sampletypederivatetype table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the sampletypederivatetype table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records. 
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM sampletypederivatetype " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the SampletypederivatetypeBean pObject into the database.
     *
     * @param pObject the SampletypederivatetypeBean pObject to be saved
     */
    //100
    public SampletypederivatetypeBean save(SampletypederivatetypeBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE 
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into sampletypederivatetype (");
    
                if (pObject.isSampletypeidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("sampletypeid");
                    _dirtyCount++;
                }

                if (pObject.isDerivatetypeidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("derivatetypeid");
                    _dirtyCount++;
                }

                if (pObject.isStandartModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("standart");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isSampletypeidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getSampletypeid());
                }
    
                if (pObject.isDerivatetypeidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getDerivatetypeid());
                }
    
                if (pObject.isStandartModified()) {
                    Manager.setBoolean(ps, ++_dirtyCount, pObject.getStandart());
                }
    
                ps.executeUpdate();
    
                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else 
            { // UPDATE 
                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE sampletypederivatetype SET ");
                boolean useComma=false;

                if (pObject.isSampletypeidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("sampletypeid").append("=?");
                }

                if (pObject.isDerivatetypeidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("derivatetypeid").append("=?");
                }

                if (pObject.isStandartModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("standart").append("=?");
                }
                _sql.append(" WHERE ");
                _sql.append("sampletypederivatetype.sampletypeid=? AND sampletypederivatetype.derivatetypeid=?");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isSampletypeidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getSampletypeid());
                }

                if (pObject.isDerivatetypeidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getDerivatetypeid());
                }

                if (pObject.isStandartModified()) {
                      Manager.setBoolean(ps, ++_dirtyCount, pObject.getStandart());
                }
    
                if (_dirtyCount == 0) {
                     return pObject;
                }
    
                Manager.setInteger(ps, ++_dirtyCount, pObject.getSampletypeid());
                Manager.setInteger(ps, ++_dirtyCount, pObject.getDerivatetypeid());
                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }
    
            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of SampletypederivatetypeBean pObjects into the database.
     *
     * @param pObjects the SampletypederivatetypeBean pObject table to be saved
     * @return the saved SampletypederivatetypeBean array.
     */
    //65
    public SampletypederivatetypeBean[] save(SampletypederivatetypeBean[] pObjects) throws SQLException 
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique SampletypederivatetypeBean pObject from a template one giving a c
     *
     * @param pObject the SampletypederivatetypeBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public SampletypederivatetypeBean loadUniqueUsingTemplate(SampletypederivatetypeBean pObject) throws SQLException
    {
         SampletypederivatetypeBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of SampletypederivatetypeBean from a template one.
     *
     * @param pObject the SampletypederivatetypeBean template to look for
     * @return all the SampletypederivatetypeBean matching the template
     */
    //88
    public SampletypederivatetypeBean[] loadUsingTemplate(SampletypederivatetypeBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from sampletypederivatetype WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;
    
             if (pObject.isSampletypeidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("sampletypeid= ?");
             }
    
             if (pObject.isDerivatetypeidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("derivatetypeid= ?");
             }
    
             if (pObject.isStandartModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("standart= ?");
             }
    
             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;
    
             if (pObject.isSampletypeidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getSampletypeid());
             }
    
             if (pObject.isDerivatetypeidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getDerivatetypeid());
             }
    
             if (pObject.isStandartModified()) {
                 Manager.setBoolean(ps, ++_dirtyCount, pObject.getStandart());
             }
    
             ps.executeQuery();
             return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a SampletypederivatetypeBean template.
     *
     * @param pObject the SampletypederivatetypeBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(SampletypederivatetypeBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;
    
        try 
        {
            sql = new StringBuffer("DELETE FROM sampletypederivatetype WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isSampletypeidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("sampletypeid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isDerivatetypeidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("derivatetypeid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isStandartInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("standart").append("=?");
                _dirtyAnd ++;
            }
    
            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;
    
            if (pObject.isSampletypeidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getSampletypeid());
            }
    
            if (pObject.isDerivatetypeidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getDerivatetypeid());
            }
    
            if (pObject.isStandartInitialized()) {
                Manager.setBoolean(ps, ++_dirtyCount, pObject.getStandart());
            }
    
            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT 
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table sampletypederivatetype.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table sampletypederivatetype with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from sampletypederivatetype " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;    
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table sampletypederivatetype with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific SampletypederivatetypeBean pObject given a c
     *
     * @param pObject the SampletypederivatetypeBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(SampletypederivatetypeBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;
    
        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from sampletypederivatetype WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;
    
                if (pObject.isSampletypeidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("sampletypeid= ?");
                }
    
                if (pObject.isDerivatetypeidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("derivatetypeid= ?");
                }
    
                if (pObject.isStandartModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("standart= ?");
                }
    
                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");
    
                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
    
                _dirtyCount = 0;
    
                if (pObject.isSampletypeidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getSampletypeid());
                }
    
                if (pObject.isDerivatetypeidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getDerivatetypeid());
                }
    
                if (pObject.isStandartModified()) {
                    Manager.setBoolean(ps, ++_dirtyCount, pObject.getStandart());
                }
    
                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the sampletypederivatetype on a SampletypederivatetypeBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting SampletypederivatetypeBean pObject
     */
    //72
    public SampletypederivatetypeBean decodeRow(ResultSet rs) throws SQLException
    {
        SampletypederivatetypeBean pObject = createSampletypederivatetypeBean();
        pObject.setSampletypeid(Manager.getInteger(rs, 1));
        pObject.setDerivatetypeid(Manager.getInteger(rs, 2));
        pObject.setStandart(Manager.getBoolean(rs, 3));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the sampletypederivatetype table on a SampletypederivatetypeBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting SampletypederivatetypeBean pObject
     */
    //73
    public SampletypederivatetypeBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        SampletypederivatetypeBean pObject = createSampletypederivatetypeBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_SAMPLETYPEID:
                    ++pos;
                    pObject.setSampletypeid(Manager.getInteger(rs, pos));
                    break;
                case ID_DERIVATETYPEID:
                    ++pos;
                    pObject.setDerivatetypeid(Manager.getInteger(rs, pos));
                    break;
                case ID_STANDART:
                    ++pos;
                    pObject.setStandart(Manager.getBoolean(rs, pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of SampletypederivatetypeBean 
     */
    //41
    public SampletypederivatetypeBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of SampletypederivatetypeBean 
     */
    public SampletypederivatetypeBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else 
                    v.add(decodeRow(rs, fieldList));
            }
            return (SampletypederivatetypeBean[])v.toArray(new SampletypederivatetypeBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER 
    ///////////////////////////////////////////////////////////////////////
    private SampletypederivatetypeListener listener = null;

    /**
     * Registers a unique SampletypederivatetypeListener listener.
     */
    //66.5
    public void registerListener(SampletypederivatetypeListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the SampletypederivatetypeBean pObject.
     *
     * @param pObject the SampletypederivatetypeBean pObject to be saved
     */
    //67
    void beforeInsert(SampletypederivatetypeBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the SampletypederivatetypeBean pObject.
     *
     * @param pObject the SampletypederivatetypeBean pObject to be saved
     */
    //68
    void afterInsert(SampletypederivatetypeBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the SampletypederivatetypeBean pObject.
     *
     * @param pObject the SampletypederivatetypeBean pObject to be updated
     */
    //69
    void beforeUpdate(SampletypederivatetypeBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the SampletypederivatetypeBean pObject.
     *
     * @param pObject the SampletypederivatetypeBean pObject to be updated
     */
    //70
    void afterUpdate(SampletypederivatetypeBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS  
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 
}
